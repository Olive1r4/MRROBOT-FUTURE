# ============================================
# DOCKER COMPOSE OVERRIDE - EXEMPLO
# ============================================
# Este arquivo pode ser usado para sobrescrever configurações
# localmente sem modificar o docker-compose.yml principal
#
# Copie para docker-compose.override.yml e customize
# ============================================

version: '3.8'

services:
  scalping-bot:
    # Sobrescrever porta
    # ports:
    #   - "8001:8000"
    
    # Adicionar volumes extras
    # volumes:
    #   - ./custom_config:/app/config
    
    # Alterar recursos
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '4'
    #       memory: 4G
    
    # Variáveis de ambiente extras
    # environment:
    #   - CUSTOM_VAR=value
    
    # Comandos customizados
    # command: python -m src.main --custom-flag

# ============================================
# EXEMPLO DE USO
# ============================================
#
# 1. Copie este arquivo:
#    cp docker-compose.override.yml.example docker-compose.override.yml
#
# 2. Edite conforme necessário
#
# 3. O Docker Compose automaticamente aplica o override:
#    docker-compose up -d
#
# ============================================
